@{
  Layout = "_Layout";
}

@using UniversityRegistrar.Models
@model UniversityRegistrar.Models.Department

<h2>Department Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.CourseName): @Html.DisplayFor(model => model.CourseName)</h3>

@if(@Model.JoinEntities.Count == 0)
{
  <p>This course does not belind to any department </p>
}
else
{
  <h4>Courses the department belongs to:</h4>
  <ul>
  @foreach(CourseDepartment join in Model.JoinEntities)
  {
    <li>@join.Course.Description</li>
    @using (Html.BeginForm("DeleteJoin", "Departments"))
    {
      @Html.Hidden("joinId", @join.CourseDepartmentId)
      <input type="submit" value="Delete"/>
    }
  }
  </ul>
}

<p>@Html.ActionLink("Back to list", "Index")</p>
<!-- <p>@Html.ActionLink("Add a Course", "AddCourse") , new { id = Model.CourseDepartment })</p> -->
<p>@Html.ActionLink("Add a Course", "AddCourse")</p>
<p>@Html.ActionLink("Edit Department", "Edit", new { id = Model.DepartmentId })</p>
<p>@Html.ActionLink("Delete Department", "Delete", new { id = Model.DepartmentId })</p>